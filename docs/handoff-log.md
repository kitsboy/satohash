# Handoff log (newest first)

Append a new `## Session — YYYY-MM-DD` at the **top** after each session.

---

## Session — 2026-07-28 (Grok)

**Done:**
- Diagnosed satohash.io apex edge poison (HTML cached as JS); purged by Cam; bundles under `/b/*`
- Landing: free OTS explainer + future Free / 21 sats / Pro pack sketch (`#free-and-fees`)
- Pricing page EN copy aligned
- Docs: started consolidation (`AGENTS.md`, `docs/deploy.md`, `docs/architecture.md`, `docs/ops-runbook.md`, this log)

**Decisions:**
- Free today = OTS calendars + Bitcoin; later fee = Lightning **to us**, same proofs
- Prefer one CF deploy path; purge zone satohash.io if edge poison returns

**Git:** see `git log -5 --oneline`

**Still open:**
- Full doc file moves into `docs/marketing/` + single archive
- `server/index.js` route split
- `src/components` / `src/pages` folder restructure

---

## Legacy

Older mega-handoffs remain in:

- `docs/KIMI-HANDOFF.md`
- `docs/KIMI-HANDOFF-2026-07-20-MEGA.md`
- `docs/archive/` (as files are moved)

Do not delete legacy handoffs without Cam approval.
